# User Permissions Frontend

This is the React frontend for the User Permissions CRUD App.

## Setup

1. Install dependencies:
   ```
   npm install
   ```
2. Start the app:
   ```
   npm start
   ```
   The app will run on http://localhost:3000
